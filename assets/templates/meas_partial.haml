.pure-u-1-3
  %a{href: "#/measurements/" + meas.name, class: "meas-button pure-button button-xlarge"}
    .meas-name
      = meas.name
    .meas-value
      = Number(parseFloat(meas.value)).toFixed(2)
